name: Sync Fork

on:
  schedule:
    - cron: '0 18 * * *' # 
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.9
        with:
          owner: Temparo
          repo: csu-index
          base: main
          head: main
